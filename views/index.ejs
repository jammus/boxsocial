<div class="row">
    <div class="twelvecol">
        <h1>boxsocial <span class="subheader">- party mode for Last.fm.</span></h1>
    </div>
</div>
<div class="row">
    <div class="eightcol siteintro">
        <p>
            Whether you're at a party, a bar or hanging out with friends you can scrobble your host's plays to your own profile in real-time.
            <a href="/about" class="readmore">Read more...</a>
        </p>
        <%
        if (!party) {
            %>
            <div class="controls">
                <a href="/join" class="button">Join a Party</a>
            </div>
            <%
        }
        else {
            %>
            <div id="current_party">
                You are at <a href="/party/<%= party.host %>"><%= party.host %>'s party</a>.
            </div>
            <%
        }
        %>
        <%- partial("howitworks") %>
    </div>
    <div class="fourcol last">
        <h3>Partying now</h3>
        <ul class="currentparties">
            <%- partial("currentparties") %>
        </ul>
    </div>
</div>

