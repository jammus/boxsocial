<div class="row">
    <div class="twelvecol">
        <h1>Join a Party</h1>
    </div>
</div>
<div class="row">
    <div class="twelvecol">
        <p>
            <% 
            if (guest) {
                %>
                Hello <b><%= guest.session.user %></b>.
                <%
            }
            %>
            To join a party please enter your <a href="/glossary#host">host</a>'s Last.fm username.
        </p>
        <form action="/join" method="post">
            <fieldset>
                <label for="host">Host's username:</label>
                <input type="text" name="host" id="host" />
                <input type="submit" value="Join" />
            </fieldset>
        </form>
    </div>
</div>
